<div class="text-box">
<h1>
    Which has the higher average box office?
</h1>
</div>

<% if @reveal %>
<div class="text-box reveal-box">
<table class="table">
<thead>
<tr>
<th>
Benedict Cumberbatch
</th>
<th>
X-Men
</th>
</tr>
</thead>
<tbody>
<tr>
<td><%= "$#{@averages.fetch("Benedict Cumberbatch").to_s.reverse.chars.each_slice(3).map(&:join).join(',').reverse}" %></td>
<td><%= "$#{@averages.fetch("X-Men").to_s.reverse.chars.each_slice(3).map(&:join).join(',').reverse}" %></td>
</tr>
</tbody>
</table>
</div>
<% else %>
<div class="text-box reveal-box">
    <a href="/2/reveal">Reveal</a>
    </div>
<% end %>

<% if @reveal %>
<div class="next-box text-box">
    <a href="/3">Next</a>
    </div>
<% end %>